import { h } from 'preact';

const Header = () => {
    return (
        <header className="app-header">
            <h1>Trading Journal</h1>
        </header>
    );
};

export default Header;

// Made with Bob
